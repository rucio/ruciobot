name: RucioBot Maintenance Checks

on:
  schedule:
    # Three runs daily (roughly 03:30, 09:30, and 15:30 CEST in practice).
    # The off-peak minute avoids the top-of-the-hour congestion that delays
    # GitHub's best-effort cron by hours.
    - cron: '23 1,7,13 * * *'
  workflow_dispatch:      # Allow manual trigger

jobs:
  maintenance-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install uv
        uses: astral-sh/setup-uv@eac588ad8def6316056a12d4907a9d4d84ff7a3b # v7.3.0

      - name: Install RucioBot
        run: uv sync

      # The checks run in precedence order (failing-tests > needs-rebase >
      # stale), so the labels a higher-priority check applies are already in
      # place when a lower-priority check decides whether to defer.
      - name: Run Failing Tests Check
        env:
          RUCIO_BOT_APP_ID: ${{ secrets.RUCIO_BOT_APP_ID }}
          RUCIO_BOT_PRIVATE_KEY: ${{ secrets.RUCIO_BOT_PRIVATE_KEY }}
        run: uv run ruciobot --action failing-tests --repo rucio/rucio

      - name: Run Needs-Rebase Check
        env:
          RUCIO_BOT_APP_ID: ${{ secrets.RUCIO_BOT_APP_ID }}
          RUCIO_BOT_PRIVATE_KEY: ${{ secrets.RUCIO_BOT_PRIVATE_KEY }}
        run: uv run ruciobot --action needs-rebase --repo rucio/rucio

      - name: Run Stale Check
        env:
          RUCIO_BOT_APP_ID: ${{ secrets.RUCIO_BOT_APP_ID }}
          RUCIO_BOT_PRIVATE_KEY: ${{ secrets.RUCIO_BOT_PRIVATE_KEY }}
        run: uv run ruciobot --action stale --repo rucio/rucio
